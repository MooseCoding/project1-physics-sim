#include <stdio.h> 
#include <string.h>
#include "renderer.h"

void clear() {
    printf("\033[2J");
}

void draw_particles(Particle* particles, int count, int width, int height) {
    for (int i = 0; i < count; i++ ) {
        
    }
} 